#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int a[2][3] = {{1,2,3},{4,5,6}};
/*
    //1、指针指向某个元素
    int *p = &a[0][0];

    *p == a[0][0];//指针指向某个变量，*这个指针就是这个变量本身；
    printf("%d,%d\n",*(p+1),p[2]);//*(p+n) == p[n];
*/
/*
    //2、指针指向某个一维数组
    int (*p)[3] = &a[0];
    *p == a[0];
    printf("%d,%d,%d\n",(*p)[1],(*(p+1))[1],p[0][1]);
*/
    //3、指针指向二维数组
    int (*p)[2][3] = &a;
    *p == a;
    printf("%d,%d\n",(*p)[1][0],(*p)[0][0]);

    return 0;
}
